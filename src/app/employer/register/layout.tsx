import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Employer Sign Up | Join Red-Flagged Verification Network',
  description:
    'Create a Red-Flagged employer account to join the shared verification network, track risky candidates, and reduce hiring no-shows.',
}

export default function EmployerRegisterLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}


